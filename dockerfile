# Используйте официальный образ Golang в качестве базового образа
FROM golang:latest

# Установите рабочую директорию внутри контейнера
WORKDIR /app

# Скопируйте файлы вашего приложения в контейнер
COPY . .

# Соберите приложение
RUN go build -o main .

# Укажите, какой порт будет использоваться вашим приложением
EXPOSE 8000

# Команда, которая будет запускать ваше приложение при запуске контейнера
CMD ["./main"]
